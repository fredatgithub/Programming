#include <iostream>

int main() {
    std::cout << "Hello, programming!\n";
    std::cout << "Here we go!\n";

    std::cin.get(); // This is used so the console does't just close instantly.
    return 0;
}