#include <iostream>
#include <string.h>

int main(int argc, char const *argv[]) {

	std::string name = "Blake"; 	// string for strings of text
	int number_of_steps = 46;		// int for integers
	double flying_time = 3.5;		// double for floating point numbers
	char decimal_point = '.';		// char for single lettered chars
	bool tap_on = true;				// bool for logical variables

	std::cin.get();
	return 0;
}